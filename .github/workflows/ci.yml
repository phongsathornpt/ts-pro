name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 2
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: Check formatting of changed Go files
        shell: bash
        run: |
          mapfile -t changed_go < <(git diff --name-only HEAD^1 HEAD -- '*.go')
          if ((${#changed_go[@]} == 0)); then
            exit 0
          fi
          unformatted="$(gofmt -l "${changed_go[@]}")"
          if [[ -n "$unformatted" ]]; then
            echo "The following changed files require gofmt:"
            echo "$unformatted"
            exit 1
          fi
      - name: Vet
        run: CGO_ENABLED=0 go vet ./...
      - name: Test main shard
        run: bash ./scripts/test-ci.sh main

  wintertc-serial:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: true
      - name: Test timing-sensitive WinterTC shard
        run: bash ./scripts/test-ci.sh wintertc-serial
